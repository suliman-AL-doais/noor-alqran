const fs = require('fs');
let store = fs.readFileSync('src/store/useStore.ts', 'utf8');

// I will just rewrite the file safely by fetching it from git or regex fixing.
// Let's just create it properly.
